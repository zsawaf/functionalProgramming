%%Given a list, return the last element of that list.
findLast([Elem], Elem).
findLast([_|Tail], Elem) :- 
  findLast(Tail, Elem).
 
%%Return true if a path is valid, false otherwise. A path is considered valid
%%iff the last element of a list and the first element of the list proceeding it are
%%equal to each other.
pathValid([], Bool).
pathValid([First, [FirstSecond|_] | T], Bool):-
  findLast(First, Last).
  pathValid(T, Y==FirstSecond).